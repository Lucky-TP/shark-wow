export enum TransactionType {
    DONATE = "donate",
    FUNDING = "funding",
}

export enum ProjectStatus {
    DRAFT = 0,
    PENDING = 1,
    APPROVED = 2,
    LAUNCH = 3,
    END = 4,
}

export enum BlogType {
    TEXT = 0,
    TEXT_BOLD = 1,
    IMAGE = 2,
}