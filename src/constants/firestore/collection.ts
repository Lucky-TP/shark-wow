export enum CollectionPath {
    METADATA = "/metadata",
    USER = "/users",
    PROJECT = "/projects",
    TRANSACTION = "/transactions",
    COMMENT = "/comments",
    REPLY = "/replies",
    ORDER = "/orders",
}
